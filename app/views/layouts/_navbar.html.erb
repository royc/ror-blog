<% if !current_author %>
  <li> <%= link_to("註冊", new_author_registration_path) %> </li>
  <li> <%= link_to("登入", new_author_session_path) %> </li>
<% else %>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      Hi!, <%= current_author.email %>
      <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
      <li> <%= link_to("登出", destroy_author_session_path, method: :delete) %> </li>
    </ul>
  </li>
<% end %>
